# 技术债务分析文档

> 本文档详细分析了项目当前存在的技术债务，并给出处理优先级和解决方案建议。
> 本版本已与实际源码逐项核对，修正了数字、代码示例与文件引用。
> 最后更新时间：2026年6月

---

## 执行摘要

当前项目处于**底层构建阶段**，这是处理技术债务的最佳时机。分析识别出 **10 个技术债务项**（归为架构、代码质量、测试、性能、平台、错误处理、文档七大类）。

**建议**：在现阶段优先处理高优先级债务（P0-P1），避免债务累积导致后期重构成本激增。

---

## 技术债务分类

### 一、架构与设计债务 (P0-P1)

#### TD-001: 缺乏 app 级统一状态管理方案
**严重程度**: P0（高）

**问题描述**:
- 项目在 `pubspec.yaml` 引入了 `provider: ^6.1.1` 依赖，但**全项目未实际使用**（无 `import 'package:provider'`、无 `MultiProvider`、无 `context.watch`）
- UI 状态主要通过原始 `setState` 管理，实测全项目 **108 处** setState 调用（`canvas_editor.dart` 27、`building_detail_dialog.dart` 14、`editor_page.dart` 14 为主）
- 需要说明的是：项目**已用** `ChangeNotifier` 管理仿真引擎（`SimulationEngine extends ChangeNotifier`，`editor_page.dart` 通过 `addListener` 手动消费 tick），但这只覆盖仿真数据，app 级状态（项目、选区、设备放置等）仍依赖 setState + 回调
- 状态共享通过回调函数层层传递，例如 `CanvasEditor` widget 暴露了一组回调：

**影响**:
```dart
// lib/canvas/canvas_editor.dart - CanvasEditor 的回调属性
class CanvasEditor extends StatefulWidget {
  final ValueChanged<ProjectState> onProjectChanged;
  final VoidCallback? onBuildingPlaced;
  final ValueChanged<PlacedBuilding?>? onBuildingSelected;
  final ValueChanged<String>? onErrorToast;
  // ...
}
```

**建议解决方案**:
```dart
// 建议做法 - Provider 状态管理
class ProjectProvider extends ChangeNotifier {
  ProjectState _project = ProjectState();
  // 状态统一管理
}

// 在顶层注册
MultiProvider(
  providers: [
    ChangeNotifierProvider(create: (_) => ProjectProvider()),
    ChangeNotifierProvider.value(value: simulationEngine),
  ],
  child: MaterialApp(...),
)

// 在子组件中访问
final project = context.watch<ProjectProvider>();
```

**预期收益**:
- 状态更新自动传播到所有依赖组件
- 显著减少 setState 调用与回调层数
- 代码更简洁、可测试性更高

**工作量估计**: 3-5 天

---

#### TD-002: 数据模型与业务逻辑混杂
**严重程度**: P1（中高）

**问题描述**:
- `PlacedBuilding`（`lib/models/project.dart`）同时包含纯数据字段与大量业务逻辑方法
- 输入/输出库存的增删校验（`acceptInputItem`/`consumeInputItems`/`addOutputItem` 等）、分流器/汇流器/物流桥的中转机制（`acceptIntoBuffer`/`acceptBridgeInputItem` 等）、端口连接判断（`conveyorPortConnections`）全部内聚在模型类中
- 模型类承担了过多职责，既难以作为纯数据结构序列化，也使单元测试需要构造完整对象

**代码位置**:
```dart
// lib/models/project.dart - PlacedBuilding 类（节选）
class PlacedBuilding {
  // 数据字段
  final String id;
  final Building building;
  double gridX;
  double gridY;
  int rotation;
  Map<String, int> outputItems;
  // ... 更多数据字段

  // 业务逻辑方法（与数据字段混杂在同一类）
  bool acceptInputItem(String itemId) { ... }
  bool consumeInputItems(String itemId, int amount) { ... }
  bool acceptIntoBuffer(String itemId, String direction) { ... }   // 分流器
  bool acceptBridgeInputItem(String itemId, String outputDirection) { ... } // 物流桥
  Map<String, bool> conveyorPortConnections(...) { ... }           // 端口连接
}
```

> 说明：UI 状态（如 `_isLongPressing`、`_hoveredBuilding`）已正确放在 `_CanvasEditorState` 中，与模型分离，此处不构成问题。端口连接的**计算逻辑**已集中在 `PlacedBuilding.conveyorPortConnections`，但其**缓存管理**分散在 `canvas_editor.dart`（见 TD-006）。

**建议解决方案**:
1. 分离纯数据模型与业务逻辑
2. 创建专门的 Repository/Service 处理业务逻辑
3. 模型仅保留字段与简单的数据校验

**预期收益**:
- 模型可独立测试
- 业务逻辑可复用
- 减少模型类的复杂度

**工作量估计**: 2-3 天

---

### 二、代码质量债务 (P1-P2)

#### TD-003: 代码重复严重
**严重程度**: P1（中高）

**问题描述**:
- `PortState.worldPosition` 和 `PortState.gridPosition` 两个方法（`lib/models/project.dart:20-109`）约 40 行主体逻辑完全相同，仅末行返回值不同
- 旋转矩阵计算逻辑重复

**代码位置**:
```dart
// lib/models/project.dart - 两个方法主体完全一致
Offset worldPosition(double gridX, double gridY, double cellSize,
    int gridWidth, int gridHeight, {int rotation = 0}) {
  // ... 约 40 行相同的 localGrid/旋转/坐标转换逻辑 ...
  return Offset(wx * cellSize, wy * cellSize);  // 唯一区别：乘以 cellSize
}

Offset gridPosition(double gridX, double gridY, int gridWidth, int gridHeight,
    {int rotation = 0}) {
  // ... 约 40 行完全相同的转换逻辑 ...
  return Offset(wx.floorToDouble(), wy.floorToDouble());  // 唯一区别：取整
}
```

**建议解决方案**:
```dart
// 提取公共方法
Offset _calculatePortPosition({
  required double gridX,
  required double gridY,
  required int gridWidth,
  required int gridHeight,
  required bool returnWorldCoords,
  double cellSize = 48.0,
  int rotation = 0,
}) {
  // 统一的转换逻辑
  final pos = _transform(/* ... */);
  return returnWorldCoords
    ? Offset(pos.dx * cellSize, pos.dy * cellSize)
    : Offset(pos.dx.floorToDouble(), pos.dy.floorToDouble());
}
```

**预期收益**:
- 消除约 40 行重复代码（这两个方法）
- 降低维护成本和 bug 风险

**工作量估计**: 1 天

---

#### TD-004: JSON 解析缺乏类型安全封装
**严重程度**: P2（中）

**问题描述**:
- JSON 解析处大量使用 `(json['key'] as num).toDouble()` 强制转换，无默认值、无错误提示
- 实测全项目约 **21 处**，集中在 `editor_page.dart`（8 处）与 `canvas/sim_protocol.dart`（8 处），以及 `building.dart`/`recipe.dart`/`sim_worker.dart`
- 运行时类型错误风险高（数据缺字段或缺类型时直接抛异常）

**代码位置**:
```dart
// lib/models/building.dart
relativeX: (json['relative_x'] as num).toDouble(),
relativeY: (json['relative_y'] as num).toDouble(),

// lib/models/recipe.dart
processTimeSeconds: (json['process_time_seconds'] as num).toDouble(),

// lib/canvas/sim_protocol.dart / lib/pages/editor_page.dart
gridX: (json['gridX'] as num).toDouble(),
```

**建议解决方案**:
```dart
// 创建类型安全的扩展方法
extension JsonDouble on Map<String, dynamic> {
  double getDouble(String key, [double defaultValue = 0.0]) {
    final value = this[key];
    if (value == null) return defaultValue;
    if (value is double) return value;
    if (value is int) return value.toDouble();
    if (value is num) return value.toDouble();
    throw FormatException('Expected double for key $key, got ${value.runtimeType}');
  }
}

// 使用
relativeX: json.getDouble('relative_x'),
```

**预期收益**:
- 提高类型安全性
- 更早发现数据格式错误
- 代码更简洁

**工作量估计**: 1 天

---

### 三、测试债务 (P1)

#### TD-005: 测试覆盖率极低
**严重程度**: P1（中高）

**问题描述**:
- 仅有 4 个测试文件
- 核心业务逻辑（传送带推进、仿真引擎、分流器/汇流器分配）无测试覆盖
- Widget 测试仅为基础示例

**当前状态**:
```
test/
├── canvas/
│   ├── grid_painter_test.dart     # 基础测试
│   └── production_flow_test.dart  # 基础测试
├── models/
│   └── project_test.dart          # 基础测试
└── widget_test.dart               # 基础测试
```

**建议解决方案**:
1. 为核心模型添加单元测试
   - `PlacedBuilding` 端口计算、分流/汇流/物流桥机制
   - `ConveyorBelt` 物品段（`ConveyorItemSegment`）推进逻辑
   - 配方解析
2. 为业务逻辑添加测试
   - 传送带推送/推进逻辑（`pushSourceItem`/`advanceItemSegments`）
   - 分流器/汇流器分配逻辑
3. 添加 Widget 测试
   - 设备弹窗
   - 配方选择面板

**目标覆盖率**: 核心业务逻辑 > 80%

**工作量估计**: 5-7 天

---

### 四、性能债务 (P2)

#### TD-006: 端口连接缓存需手动触发
**严重程度**: P2（中）

**问题描述**:
- `_rebuildPortConnectionsCache()`（`lib/canvas/canvas_editor.dart:118`）需要在多处手动调用，实测至少在 8 处触发（建筑放置、移动、删除、旋转、传送带创建等）
- 容易遗漏导致状态不一致
- 每次重建全量计算，性能浪费

**代码位置**:
```dart
// lib/canvas/canvas_editor.dart
void _rebuildPortConnectionsCache() {
  portConnectionsCache = {};
  for (final pb in _project.buildings) {
    portConnectionsCache[pb.id] = pb.conveyorPortConnections(
      _project.conveyors, ...);
  }
}

// 在多处手动调用（节选）
void initState() { ... _rebuildPortConnectionsCache(); }
void _onBuildingPlaced() { ... _rebuildPortConnectionsCache(); }
```

**建议解决方案**:
```dart
// 实现自动缓存机制
class PortConnectionCache {
  final ProjectState _project;
  Map<String, Map<String, bool>> _cache = {};
  int _projectVersion = 0;

  Map<String, bool> get(String buildingId) {
    if (_project.version != _projectVersion) {
      _rebuild();  // 自动检测并重建
    }
    return _cache[buildingId] ?? {};
  }

  void invalidate(String buildingId) {
    _cache.remove(buildingId);  // 精细失效
  }
}
```

**预期收益**:
- 减少全量重建次数
- 避免缓存不一致 bug
- 自动化维护

**工作量估计**: 2 天

---

#### TD-007: 魔法数字散布
**严重程度**: P2（中）

**问题描述**:
- `cellSize = 48.0` 在多个文件各自重复定义，实测至少 7 处
- 缺乏统一的常量管理

**代码位置**:
```dart
// 散布在多个文件
static const double cellSize = 48.0;   // lib/AIC/Logistics Units/transport_belt.dart:65
static const double cellSize = 48.0;   // lib/canvas/canvas_editor.dart:69
static const double _cellSize = 48.0;  // lib/canvas/simulation_engine.dart:28
static const double _cellSize = 48.0;  // lib/canvas/sim_worker.dart:28
static const double cellSize = 48.0;   // lib/models/project.dart:563
const cellSize = 48.0;                 // lib/widgets/building_depot_panel.dart:54
// project.dart:405 还有一处方法默认参数 double cellSize = 48.0
```

**建议解决方案**:
```dart
// lib/constants/ui_constants.dart
class UiConstants {
  static const double cellSize = 48.0;
  static const double minScale = 0.25;
  static const double maxScale = 5.0;
  static const double minScaleForBuildingPlacement = 0.5;
}

class SimulationConstants {
  static const double tickRate = 20.0;              // simulation_engine.dart:27
  static const double portConnectionThreshold = 30.0; // simulation_engine.dart:29
  static const int maxInputItemCount = 50;          // project.dart:113
  static const int maxOutputItemCount = 50;         // project.dart:114
}
```

**预期收益**:
- 统一配置管理
- 便于全局调整
- 提高代码可读性

**工作量估计**: 1 天

---

### 五、平台兼容债务 (P2)

#### TD-008: Isolate 处理逻辑复杂
**严重程度**: P2（中）

**问题描述**:
- Web 平台不支持 Isolate，需要单独处理
- `SimulationEngine`（`lib/canvas/simulation_engine.dart`）中原生 Isolate 逻辑与 Web 主线程回退逻辑混杂在同一类内（`_fallback*` 系列方法约 10 个与原生路径并存）
- 回退模式代码路径复杂，难以测试

**代码位置**:
```dart
// lib/canvas/simulation_engine.dart
Future<void> init() async {
  if (kIsWeb) {
    // Web 平台回退
    return;
  }
  try {
    _isolate = await Isolate.spawn(simWorkerEntry, _mainReceivePort!.sendPort);
  } catch (e) {
    debugPrint('[SimulationEngine] Isolate 初始化失败，回退到主线程模式: $e');
    // 失败后回退
  }
}
// 同一类内还有 _fallbackUpdateConveyors / _fallbackUpdateBuildings / _fallbackPushOutputToBelts 等回退实现
```

**建议解决方案**:
```dart
// 创建平台抽象接口
abstract class SimulationWorker {
  Future<void> init();
  Future<void> attach(ProjectState project);
  void start();
  void stop();
}

// 原生平台实现
class IsolateSimulationWorker implements SimulationWorker { ... }

// Web 平台实现
class MainThreadSimulationWorker implements SimulationWorker { ... }

// 工厂方法
SimulationWorker createWorker() {
  return kIsWeb
    ? MainThreadSimulationWorker()
    : IsolateSimulationWorker();
}
```

**预期收益**:
- 逻辑分离，更易测试
- 添加新平台更容易
- 降低主文件复杂度

**工作量估计**: 3 天

---

### 六、错误处理债务 (P1)

#### TD-009: 缺乏统一错误处理机制
**严重程度**: P1（中高）

**问题描述**:
- 全项目实测约 24 处 `debugPrint`，仅开发环境可见，生产环境不可见
- 无错误分类和上报（项目中无 `ErrorSeverity`/`AppError` 之类的抽象）
- JSON 解析、资源加载失败时缺乏友好提示，仅打印日志

**当前做法**:
```dart
// lib/main.dart - SVG 预加载（真实代码）
Future<void> _precacheSvg(String path) async {
  try {
    await vg.loadPicture(SvgAssetLoader(path), null);
  } catch (e) {
    debugPrint('[precache] SVG 加载失败: $path -> $e');  // 仅开发环境可见
  }
}
```

**建议解决方案**:
```dart
// lib/utils/error_handler.dart
enum ErrorSeverity { info, warning, error, critical }

class AppError {
  final String message;
  final ErrorSeverity severity;
  final String? code;
  final StackTrace? stackTrace;

  void report() {
    // 统一上报逻辑
    debugPrint('[$severity] $code: $message');
    if (severity == ErrorSeverity.critical) {
      // 发送到崩溃服务
    }
  }
}

// 使用
try {
  await operation();
} catch (e, stackTrace) {
  AppError(
    message: 'SVG 加载失败',
    severity: ErrorSeverity.warning,
    code: 'SVG_LOAD_FAILED',
    stackTrace: stackTrace,
  ).report();
}
```

**预期收益**:
- 生产环境错误可见
- 便于问题追踪
- 用户体验更好

**工作量估计**: 2 天

---

### 七、文档债务 (P2)

#### TD-010: 文档注释覆盖不均
**严重程度**: P2（中）

**问题描述**:
- 核心模型文档相对完善（`lib/models/project.dart` 实测约 36 处 `///` 注释，分流/汇流/物流桥机制均有详细说明），但**覆盖不均**：
  - `canvas_editor.dart`（11 处）、`simulation_engine.dart`（6 处）相对偏少
  - 复杂算法（如 `belt_direction_utils.dart` 的 BFS 寻路）有粗略注释但缺乏完整算法说明
- 公开 API 无统一的使用示例
- 部分魔法值的含义（如 `freezeProgress` 的 `-3.0`/`-0.75`/`-1.0` 等哨兵值）依赖行内注释，未集中说明

**建议解决方案**:
1. 为所有公开类补充文档注释，补齐 `canvas_editor`/`simulation_engine` 的薄弱处
2. 为复杂方法添加算法说明（BFS 寻路、物品段推进 `advanceItemSegments`）
3. 为关键哨兵值（`freezeProgress` 的各特殊值）集中列表说明含义

**示例**:
```dart
/// 计算端口在世界坐标系中的位置。
///
/// 考虑了建筑旋转和网格尺寸，返回相对于画布左上角的像素坐标。
///
/// 参数:
/// - [gridX]: 建筑中心网格 X 坐标
/// - [gridY]: 建筑中心网格 Y 坐标
/// - [cellSize]: 单元格像素尺寸，默认 48.0
/// - [rotation]: 旋转次数（0-3），每次旋转 90 度
///
/// 返回: 端口在世界坐标系中的位置（像素）
Offset worldPosition(double gridX, double gridY, ...);
```

**工作量估计**: 2-3 天

---

## 优先级建议

### 立即处理 (P0)
| ID | 问题 | 工作量 | 收益 |
|----|------|--------|------|
| TD-001 | 缺乏 app 级统一状态管理 | 3-5天 | 高 |

### 近期处理 (P1) - 1-2周内
| ID | 问题 | 工作量 | 收益 |
|----|------|--------|------|
| TD-002 | 数据模型与业务逻辑混杂 | 2-3天 | 高 |
| TD-003 | 代码重复严重 | 1天 | 中 |
| TD-005 | 测试覆盖率极低 | 5-7天 | 高 |
| TD-009 | 缺乏统一错误处理 | 2天 | 中 |

### 中期处理 (P2) - 1个月内
| ID | 问题 | 工作量 | 收益 |
|----|------|--------|------|
| TD-004 | JSON 解析缺乏类型安全封装 | 1天 | 中 |
| TD-006 | 端口连接缓存需手动触发 | 2天 | 中 |
| TD-007 | 魔法数字散布 | 1天 | 低 |
| TD-008 | Isolate 处理逻辑复杂 | 3天 | 中 |
| TD-010 | 文档注释覆盖不均 | 2-3天 | 低 |

---

## 总体建议

### 为什么现在处理技术债务？

1. **成本更低**：代码规模较小时重构成本远低于后期
2. **影响更小**：功能未完全定型，重构不会破坏现有流程
3. **效率提升**：良好的架构会让后续开发更高效

### 处理策略

1. **分批处理**：按优先级分批次，避免一次性改动过大
2. **测试先行**：在重构前为即将修改的代码添加测试
3. **渐进迁移**：新旧代码并存，逐步替换
4. **文档同步**：每次重构后更新相关文档

### 总工作量估计

- **P0 债务**: 3-5 天
- **P1 债务**: 10-13 天
- **P2 债务**: 9-10 天
- **总计**: 约 22-28 天（约 1-1.5 个月）

---

*最后更新时间：2026年6月*
